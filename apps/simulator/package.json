{
  "name": "@gridlens/simulator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "lint": "echo \"No simulator lint config yet\"",
    "test": "echo \"No simulator tests yet\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "tsx": "^4.19.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
