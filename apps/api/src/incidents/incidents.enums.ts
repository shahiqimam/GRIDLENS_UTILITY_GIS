export enum IncidentPriority {
  P1 = "P1",
  P2 = "P2",
  P3 = "P3"
}

export enum IncidentSource {
  DetectedFault = "DETECTED_FAULT"
}

export enum IncidentStatus {
  Open = "OPEN",
  Acknowledged = "ACKNOWLEDGED",
  Resolved = "RESOLVED"
}